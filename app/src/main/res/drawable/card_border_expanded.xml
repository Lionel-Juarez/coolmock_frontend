<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/white"/> <!-- Fondo blanco para coherencia -->
    <stroke
        android:width="2dp"
        android:color="@color/expandedBorderColor"/> <!-- Color distintivo para estado expandido -->
    <corners
        android:radius="30dp"/> <!-- Esquinas redondeadas consistentes -->
</shape>
